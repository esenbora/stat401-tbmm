# Dashboard-only dependencies (Streamlit Community Cloud reads this root file).
# The dashboard reads pre-computed parquet/JSON snapshots — NO Spark at view time,
# so it needs only these light packages. For the full analysis pipeline see
# requirements-pipeline.txt.
streamlit>=1.30.0
pandas>=2.1.0
pyarrow>=14.0.0
plotly>=5.18.0
